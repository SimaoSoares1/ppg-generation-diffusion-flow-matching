# ppg-generation-diffusion-flow-matching
Notebooks for comparing OT-CFM and DDPM (ancestral + DDIM) for conditional synthetic PPG signal generation, evaluated across three different datasets, with downstream atrial fibrillation (AF) classification.
